if instance_exists(owner_id)
{
    image_angle = owner_id.image_angle;
    x = owner_id.x + lengthdir_x(15, owner_id.image_angle - 40);
    y = owner_id.y + lengthdir_y(15, owner_id.image_angle - 40);
}
